import 'mocha';
import { expect } from 'chai';
import { Evento } from "../src/mod2Observable";
import { EventObservable } from "../src/mod2Observable";
import { EventObserver } from "../src/mod2Observer";


// Test suite for Mudanza class
describe('Pruebas añadiendo elementos a la colección.', () => {
  it('Notificando', () => {
    const eventoobservable = new EventObservable();
  });
  it('Añadimos 2 elementos', () => {

  });
});

describe('Pruebas Comprobando tamaño', () => {
  it('Comprobando tamaño = 2', () =>{

  });
});